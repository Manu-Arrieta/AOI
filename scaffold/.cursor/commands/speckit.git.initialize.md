# /speckit.git.initialize

<!-- Generated by `pnpm aoi:sync-rules`. Do not edit: the source is .github/prompts/speckit.git.initialize.prompt.md. -->

Read `.github/prompts/speckit.git.initialize.prompt.md` in this repository and execute it exactly as written,
following every step in order.

That file is the single definition of this command for every assistant. This
pointer exists only so `/speckit.git.initialize` resolves here the same way it resolves in
GitHub Copilot, which reads `.github/prompts` natively.
