---
description: "Create a governed folder inside .resources and persist the structural change."
---

# /new-resource-folder

<!-- Generated by `pnpm aoi:sync-rules`. Do not edit: the source is .github/prompts/new-resource-folder.prompt.md. -->

Read `.github/prompts/new-resource-folder.prompt.md` in this repository and execute it exactly as written,
following every step in order.

That file is the single definition of this command for every assistant. This
pointer exists only so `/new-resource-folder` resolves here the same way it resolves in
GitHub Copilot, which reads `.github/prompts` natively.
