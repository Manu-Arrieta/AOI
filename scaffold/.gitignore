# AOI Node runtime artifacts
node_modules/
.pnpm-store/
pnpm-lock.yaml

# Nuxt runtime artifacts
.nuxt/
.output/
dist/

# Test and cache artifacts
coverage/
*.tsbuildinfo

# ─────────────────────────────────────────────────────────────────────────────
# AOI agentic infrastructure — local-only tooling.
# Installed by AOI setup into this repo; do NOT commit it to the application repo.
# Remove it entirely with AOI teardown. Files still live on disk — they are just
# kept out of version control.
# ─────────────────────────────────────────────────────────────────────────────

# Copilot surface
.github/agents/
.github/hooks/
.github/instructions/
.github/prompts/
.github/scripts/
.github/copilot-instructions.md

# Antigravity surface
.agent/

# Spec-kit (SDD) memory, skill registry, and resources
.specify/
.atl/
.resources/

# Agentic ops dashboard
aoi_apps/agentic-ops-dashboard/

# AOI tooling scripts
scripts/sandbox/
scripts/memory-sync/

# Agent context manifests
GEMINI.md
AGENTS.md
CLAUDE.md

# Agentic runtime trail (SDD tasks, sandboxes, ICM local artifacts)
.tasks/
.sandboxes/
.icm/

# Exported memory bundle artifacts
.exportsmemories/

# NVIDIA customendpoint credentials — keep example tracked, ignore real keyfile
.vscode/ChatLanguageModel.json
!.vscode/ChatLanguageModel.example.json