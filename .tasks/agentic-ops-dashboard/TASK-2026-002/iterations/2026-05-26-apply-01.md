# Iteration Log: 2026-05-26 Apply 01

## Scope

- Establish the managed Node workspace and baseline Nuxt runtime under the live
  repository and `scaffold/`.
- Introduce the explicit `relations.json` contract and preserve it across
  `/sdd-new` and `/sdd-ff`.
- Implement the first server-side dashboard slice: authoritative workspace
  snapshot, task relation loading, SSE events, and governed `.resources/`
  operations.

## Completed

- Created root and scaffold runtime metadata, app package manifests, Nuxt shell,
  and setup or teardown parity.
- Added shared relation schema and helpers plus workflow guidance updates for
  explicit `.resources/` links.
- Implemented server utilities for registry parsing, snapshot construction,
  relation loading, workspace watching, and governed resource operations.
- Added focused Vitest coverage for relations helpers, registry parsing,
  snapshot construction, and guarded resource mutations.
- Mirrored the dashboard app directory into `scaffold/apps/agentic-ops-dashboard/`
  and confirmed parity with a directory diff excluding generated artifacts.

## Validation

- `pnpm install`
- `pnpm --filter agentic-ops-dashboard test`
- `pnpm --filter agentic-ops-dashboard exec nuxt prepare`
- `diff -qr --exclude '.nuxt' --exclude '.output' --exclude 'node_modules' apps/agentic-ops-dashboard scaffold/apps/agentic-ops-dashboard`

## Notes

- `pnpm` 11 required `allowBuilds` in `pnpm-workspace.yaml` for native postinstall
  scripts used by dashboard dependencies.
- `relations.json` remains opt-in and is populated only from explicit Owner-linked
  `.resources/userstories/*` and `.resources/workflows/*` paths.
- Governed `.resources/` mutations update `.resources/constitution.md` and refuse
  writes outside the allowed subtree.

## Next Slice

- Build the dashboard overview and task detail UI on top of the new workspace
  snapshot API.
- Render explicit task-to-resource relations in the task detail flow.
- Add the `.resources/` explorer and action dialogs against the governed APIs.