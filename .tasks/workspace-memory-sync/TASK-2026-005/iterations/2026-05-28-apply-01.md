# Iteration Log: 2026-05-28 Apply 01

## Scope

- Completar el foundation slice de `TASK-2026-005` para que el repo entienda el
  contrato bundle-aware antes de implementar exportacion e importacion reales.
- Materializar el soporte minimo de schema, templates, helpers de compresion y
  fixtures bundle-aware sobre `scripts/memory-sync/`.

## Completed

- Extendidos los manifests versionados con `sourceTransport` y `bundleMetadata`
  opcional en root, scaffold y fixtures de prueba.
- Creado `memory-bundle.template.json` en root, scaffold y fixtures validos.
- Añadidas utilidades compartidas para `gzip`, digest `sha256` y resolucion de
  rutas de exportacion dentro de `.exportsmemories/`.
- Incorporadas validaciones de schema para bundle metadata, envelopes
  comprimidos y trazabilidad de source bundle.
- Agregados fixtures completos, parciales e invalidos para bundles y un test
  dedicado `bundle-contract.test.mjs`.

## Validation

- `rtk test node --test scripts/memory-sync/*.test.mjs`

## Notes

- ICM no expone un export nativo de memorias generales, por lo que el contrato
  bundle-aware tuvo que diseñarse sobre capacidades reales de `list`, `recall`,
  `memoir export` y `feedback search`.
- `.exportsmemories/` ya existe como base gobernada de salida, pero este slice
  aun no implementa los scripts operativos de export/import.

## Next Slice

- Implementar `export-memory-bundle.mjs` y su cobertura enfocada.
- Implementar `import-memory-bundle.mjs` y extender
  `prepare-version-manifest.mjs` con metadata bundle-origin cuando la fuente sea
  offline.
- Abrir los workflows y mirrors compartidos para export/import.