# Iteration Log — 2026-05-27 — Apply 01

## Scope

Execute the foundation slice of TASK-2026-004 for version-aware memory
governance and active-version resolution.

## Completed Slices

1. Updated `.specify/memory/constitution.md` to formalize version-aware memory
   resolution, immutable manifests, and rollback-safe active pointers.
2. Added the missing `scaffold/.specify/memory/constitution.md` mirror and the
   initial `.specify/memory/versions/` tree in both root and `scaffold/`.
3. Seeded the live repository with `active.json`, a baseline manifest, a dynamic
   constitution snapshot, and reusable templates for future memory versions.
4. Implemented `scripts/memory-sync/schema.mjs` and
   `scripts/memory-sync/resolve-active-version.mjs`.
5. Added focused Node validation for schema and active-version resolution,
   including fixture-based cases for missing workspaces, missing manifests, and
   missing constitution snapshots.

## Validation

- `rtk test node --test scripts/memory-sync/*.test.mjs`

## Outcome

The repository can now describe and validate a version-aware memory baseline for
`aoi` without ambiguous bare-topic resolution. The next slice is candidate
manifest preparation plus explicit activation.