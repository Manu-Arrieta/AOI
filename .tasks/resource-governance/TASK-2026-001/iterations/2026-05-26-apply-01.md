# Iteration Log — 2026-05-26 — Apply 01

## Scope

Execute the full implementation of TASK-2026-001 for the governed `.resources/`
subsystem.

## Completed Slices

1. Added `.resources/constitution.md` in root and `scaffold/`, plus managed
   default directories for `userstories/` and `workflows/`.
2. Updated `setup.sh`, `setup.ps1`, `teardown.sh`, and `teardown.ps1` so
   `.resources/` is provisioned and removed symmetrically.
3. Amended `.specify/memory/constitution.md` to allow the subordinate
   `.resources/constitution.md` contract.
4. Updated `GEMINI.md`, `.atl/skill-registry.md`, and their `scaffold/` mirrors
   to expose the new resources surface and support workflows.
5. Hardened `/sdd-new` and `/sdd-ff` so `.resources/` is opt-in only.
6. Added `/new-resource-folder`, `/move-resource-folder`, and
   `/delete-resource-folder` across Copilot, Antigravity, and `scaffold/`.
7. Hardened `/sdd-apply` and `/sdd-verify` so `.resources/workflows/` is always
   treated as non-executable context.
8. Updated `README.md` and `README.es.md` to document the installed resources
   subsystem and the new commands.

## Validation

- `rtk bash -n setup.sh`
- `rtk bash -n teardown.sh`
- Editor diagnostics over scripts, docs, constitutions, prompts, skills, and
  scaffold mirrors reported no errors.
- Text checks confirmed the new commands and resource-governance rules are
  present across root and `scaffold/` surfaces.

## Outcome

TASK-2026-001 is implemented and ready for `/sdd-verify`.