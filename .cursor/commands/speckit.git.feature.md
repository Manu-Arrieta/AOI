# /speckit.git.feature

<!-- Generated by `pnpm aoi:sync-rules`. Do not edit: the source is .github/prompts/speckit.git.feature.prompt.md. -->

Read `.github/prompts/speckit.git.feature.prompt.md` in this repository and execute it exactly as written,
following every step in order.

That file is the single definition of this command for every assistant. This
pointer exists only so `/speckit.git.feature` resolves here the same way it resolves in
GitHub Copilot, which reads `.github/prompts` natively.
