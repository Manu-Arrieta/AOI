---
description: "Verify implementation against spec, run health checks, and decide on archive or continue."
---

# /sdd-verify

<!-- Generated by `pnpm aoi:sync-rules`. Do not edit: the source is .github/prompts/sdd-verify.prompt.md. -->

Read `.github/prompts/sdd-verify.prompt.md` in this repository and execute it exactly as written,
following every step in order.

That file is the single definition of this command for every assistant. This
pointer exists only so `/sdd-verify` resolves here the same way it resolves in
GitHub Copilot, which reads `.github/prompts` natively.
