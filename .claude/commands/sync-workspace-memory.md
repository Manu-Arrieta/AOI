---
description: "Prepare and optionally activate a governed memory sync from another workspace with explicit source version resolution."
---

# /sync-workspace-memory

<!-- Generated by `pnpm aoi:sync-rules`. Do not edit: the source is .github/prompts/sync-workspace-memory.prompt.md. -->

Read `.github/prompts/sync-workspace-memory.prompt.md` in this repository and execute it exactly as written,
following every step in order.

That file is the single definition of this command for every assistant. This
pointer exists only so `/sync-workspace-memory` resolves here the same way it resolves in
GitHub Copilot, which reads `.github/prompts` natively.
